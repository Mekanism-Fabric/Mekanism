archivesBaseName = "mekanism"
version = "1.0.0"

dependencies {
    // Test Mod Dependencies go here
}

moduleDependencies(project, [
    'mekanism-api'
])

loom {
    accessWidenerPath = file("src/main/resources/mekanism.accesswidener")
}